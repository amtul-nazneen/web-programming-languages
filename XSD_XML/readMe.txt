=====================================================================================
================= ALL THE BELOW CAN BE ACCESSED FROM Homework4.html =================
=====================================================================================
1. XML Schema
	1,2. Schema file: stocks.xsd
2. XML Documentation Creation and Display
	a. XML File: stocks.xml
	b. JSON File: stocks.json
	c. XML data on HTML: DisplayStocks.html
		Workaround to get it working in Firefox:
		The following is the workaround related to the CORS problem for loading XML file into HTML:
        1. Copy .html and .xml to the same directory in the file system
        2. Type "about:config" in the Firefox address bar
        3. Search "privacy.file_unique_origin" and double-click on the row to turn it to "false"
        4. Open .html in Firefox
    d. XPath Queries: XpathQueries.html
=======================================================================================